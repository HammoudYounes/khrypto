# Keys (git-ignored)

Devnet oracle keypair lives here, e.g. `oracle-devnet.json`
(generate with `solana-keygen new -o keys/oracle-devnet.json`).
Set in `.env`: `ORACLE_KEYPAIR_PATH=/keys/oracle-devnet.json`
Never store mainnet keys in this repository.
