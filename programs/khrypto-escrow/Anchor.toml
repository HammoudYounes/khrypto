# DEVNET ONLY — this program must never be deployed to mainnet in its
# current form (single-key oracle, no audit).
[toolchain]

[features]
resolution = true
skip-lint = false

[programs.devnet]
khrypto_escrow = "Fg6PaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/**/*.ts"
